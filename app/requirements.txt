# HF Spaces (CPU) runtime deps for the demo. The package itself is pulled from GitHub;
# torch is the CPU wheel. PhysicsNeMo is NOT needed here (the demo uses PhysicsUDE).
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.2
hydrophysics-ai @ git+https://github.com/Rekin226/HydroPhysicsAI.git@main
plotly>=5.20
gradio>=6.0
